<%= form_for([@reservation, @comment]) do |f| %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Comment!" %>
  </div>
<% end %>
